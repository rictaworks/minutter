# ユースケース図

```mermaid
flowchart TD
    actor["👤 ユーザー\n（シングルユーザー）"]

    UC01["UC01: マイクで録音する\ncpal → WAV保存"]
    UC02["UC02: 音声ファイルをインポートする\nWAV/MP3/M4A/WebM → ffmpegでWAV変換"]
    UC03["UC03: 文字起こし結果を確認・編集する\nVosk STT → 手動修正"]
    UC04["UC04: 議事録を生成・確認する\n決定事項・次回議題・本文"]
    UC05["UC05: ToDoを確認・編集する\n抽出一覧・チェック・追記・削除"]
    UC06["UC06: 要約を確認する\nTextRank抽出型要約"]
    UC07["UC07: 過去の会議記録を一覧表示する\nローカルSQLiteの永続データ"]
    UC08["UC08: 会議記録を削除する\n手動・物理削除"]
    UC09["UC09: テキスト編集後に再生成する\n手動編集 → 議事録・ToDo・要約更新"]

    actor --> UC01
    actor --> UC02
    actor --> UC03
    actor --> UC04
    actor --> UC05
    actor --> UC06
    actor --> UC07
    actor --> UC08
    actor --> UC09
```

## ユースケース依存関係

| ユースケース | 前提条件 |
|-------------|---------|
| UC03 | UC01 または UC02 の完了後に実行可能 |
| UC04, UC05, UC06 | UC03 の完了後・生成ボタン押下で一括実行 |
| UC07 | いつでも参照可能（ローカル永続データ） |
| UC08 | UC07 から実行可能 |
| UC09 | UC03 完了後・UC04〜06 完了後の任意のタイミングで実行可能 |
